name: Doctor

# Lancement manuel : Actions → Doctor → Run workflow. Ne déploie rien.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend:
    uses: ouangni-wangny/xsel-deploy-mutualise/.github/workflows/doctor.yml@v1.3.0
    with:
      app_path: backend
      deploy_path: /home/manoohpq/peci-ci.com/backend
      health_check_url: https://backend.peci-ci.com/up
    secrets:
      SSH_HOST: ${{ secrets.DEPLOY_SSH_HOST }}
      SSH_PORT: ${{ secrets.DEPLOY_SSH_PORT }}
      SSH_USER: ${{ secrets.DEPLOY_SSH_USER }}
      SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
